{
  "name": "document_translation_auto",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start": "npm run start:client",
    "start:client": "cd client && npm start",
    "build": "cd client && npm run build",
    "server": "cd api && npm run start"
  },
  "engines": {
    "node": ">=14.0.0",
    "npm": ">=10.7.0"
  },
  "proxy": "http://localhost:3001"
}
